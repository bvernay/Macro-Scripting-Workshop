/* 
	For loop
	initialisation i = 0
	condition: test if i is less than or equal to 10. 
			- if the condition is true the loop body is executed, 
			- if the condition is false the code exit the loop
	increment: add +1 to the current value of i each time the loop body is executed

	if the condition   
 */
 
for (i = 0; i <= 10; i++){
	print(i);
}
