/*
	If you don't use a loop you have to code every single action
	even the identical one that are repaeted several times
*/

//Open an image in Fiji before running the script
run("Median...", "radius=2");
run("Median...", "radius=2");
run("Median...", "radius=2");
run("Median...", "radius=2");
run("Median...", "radius=2");